#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")"
# shellcheck source=env.sh
. ./env.sh
# Logs IK. --arm-live requires this host is isengard. Still does not open CAN.
exec "$FB_PY" "$FB/controller/scripts/hw_shadow.py" --from-dir "$OUT/frames" --out "$OUT/hw-shadow.json" --arm-live
