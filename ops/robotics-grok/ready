#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")"
# shellcheck source=env.sh
. ./env.sh
export PYTHONPATH="${FB}/controller${PYTHONPATH:+:$PYTHONPATH}"
exec "${LEROBOT_PY}" "$FB/controller/scripts/hw_hover.py" --ready --out "$OUT/hw-ready.json" "$@"
